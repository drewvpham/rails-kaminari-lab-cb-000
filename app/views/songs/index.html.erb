<h1>Songs!</h1>
<%= paginate @songs %>
<%= page_entries_info @songs %>
<!-- <%= paginate @songs, window: 14 %> -->
<ul>
<% @songs.each do |song| %>
  <li><%= display_artist(song) %> - <%= link_to song.title, song_path(song) %></li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
